# requirements.txt
# Dependencies for the Renewable Energy Pub-Sub + ANN Project
#
# Core pub-sub system (broker.py, publisher.py, subscriber.py):
#   Uses only Python standard library - no extra packages needed.
#   (socket, threading, json, pickle, uuid, collections, datetime, argparse)
#
# ANN model and dataset (ann_model.py, dataset_generator.py):
#   These require the packages below.

# Data handling
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0   # MinMaxScaler used in ann_model.py

# Neural network
tensorflow>=2.13.0    # Keras is bundled with TensorFlow 2.x

# Optional - for generating training plots
# matplotlib>=3.7.0
